$kind: http-request
url: "{{baseUrl}}/health"
method: GET
scripts:
  - type: afterResponse
    code: |-
      pm.test('Health API returns 200', function () {
          pm.response.to.have.status(200);
      });
    language: text/javascript
order: 1000
